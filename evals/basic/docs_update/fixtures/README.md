# openshard

Intelligent task routing and execution across model tiers.

## Installation

```bash
pip install openshard
```

## Usage

```bash
openshard run "Your task description here"
```

## Commands

- `openshard run` — execute a task
- `openshard report` — show a summary of past runs
